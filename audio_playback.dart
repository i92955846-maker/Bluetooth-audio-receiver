// Placeholder audio playback helper (use flutter_sound in real project)
import 'package:flutter/material.dart';
class AudioPlayback { static Future<void> playRemoteStream(dynamic s) async{} static Future<void> playPCM(List<int> b) async{} static void dispose(){} }
